apiVersion: v1
kind: ConfigMap
metadata:
  name: vmagent-scrape-config
  namespace: monitoring
data:
  scrape.yaml: |
    scrape_configs:
      - job_name: traefik
        static_configs:
          - targets: ['traefik.kube-system.svc.cluster.local:9100']
